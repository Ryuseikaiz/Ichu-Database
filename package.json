{
  "name": "ichu-database-monorepo",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "npm install --prefix web && npm run build --prefix web",
    "start": "cd server && npm start",
    "seed": "cd server && npm run seed"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.6.3"
  }
}
